package offer;

import java.util.HashMap;

public class offer_35 {
    // 用于保存已经创建的新节点
    private HashMap<Node, Integer> map = new HashMap<>();
    // 新节点的额外头节点
    private  Node reshead = new Node(0);

    //public Node copyRandomList(Node head) {
    //
    //}
    //public void recurrent(Node h, Node newtail){
    //    // 如果原始链表待访问的节点为空,说明这个点访问结束了,直接返回即可
    //    if(h == null) return;
    //    if()
    //
    //
    //
    //}
}
