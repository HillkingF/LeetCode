package Linked_List;

import java.util.List;

public class Simple_206_reverseList {
    //public ListNode reverseList(ListNode head) {
    //    if(head == null) return null;
    //    ListNode pre = null;
    //    ListNode cur = head;
    //    while(cur != null){
    //        ListNode temp = cur.next;   //将待操作的后续节点存储到temp中
    //        cur.next = pre;  //将带操作的节点的指针域指向前面的节点，这样带操作的节点就和后面的节点断开了
    //        pre = cur;   //pre指针指向刚刚加入的节点
    //        cur = temp;  //cur指向的这个节点操作完毕，重新指向temp中的节点，迭代操作
    //    }
    //    return pre;
    //
    //
    //}
}
